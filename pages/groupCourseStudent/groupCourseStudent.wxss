/* pages/groupCourseStudent/groupCourseStudent.wxss */
@import '/style/iconfont.wxss';

page {
  height: 100vh;
  background-color: #f5f5f5;
}
/* 筛选标签 */
.tabs {
  display: flex;
}

.tab-item {
  flex: 1;
  text-align: center;
  font-size: 32rpx;
  color: #000;
  position: relative;
  transition: all 0.3s ease;
  height: 100rpx;
  line-height: 100rpx;
}

.tab-item.active {
  font-weight: 600;
}

.tab-item.active::after {
  content: '';
  position: absolute;
  bottom: 0;
  left: 50%;
  transform: translateX(-50%);
  width: 30rpx;
  height: 6rpx;
  background-color: #fae64f;
  border-radius: 2rpx;
}

/* 课程列表容器 */
.courses-container {
  padding: 24rpx 16rpx 200rpx 16rpx;
}

/* 课程卡片 */
.course-card {
  background: #fff;
  border-radius: 16rpx;
  margin-bottom: 24rpx;
  position: relative;
  transition: all 0.3s ease;
}
.course-card:active {
  transform: scale(0.98);
}
.course-card-wrap{
  padding: 24rpx;
  display: flex;
  gap: 20rpx;
}

/* 状态标签 */
.status-tag {
  position: absolute;
  top: 20rpx;
  left: 20rpx;
  padding: 8rpx 16rpx;
  border-radius: 8rpx;
  font-size: 24rpx;
  z-index: 10;
}

.status-tag.status_0 {
  background-color: #fa541c;
  color: #fff;
}
.status-tag.status_1 {
  background-color: #fae64f;
  color: #000;
}
.status-tag.status_2 {
  background-color: #86868b;
  color: white;
}

/* 课程图片 */
.course-image {
  width: 260rpx;
  height: 220rpx;
  border-radius: 20rpx;
  overflow: hidden;
}

.course-image image {
  width: 100%;
  height: 100%;
  object-fit: cover;
}

/* 课程信息 */
.course-info {
  flex: 1;
  height: 220rpx;
  display: flex;
  flex-direction: column;
  justify-content: space-between;
}

.course-title {
  font-size: 30rpx;
  font-weight: 600;
  color: #1d1d1f;
  line-height: 1.4;
  overflow: hidden;
  text-overflow: ellipsis;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  word-break: break-all;
}

.meta-item {
  display: flex;
  align-items: center;
  font-size: 28rpx;
  color: #86868b;
}

.meta-item .icon {
  margin-right: 10rpx;
  font-size: 24rpx;
}

.meta-item .text {
  flex: 1;
}

/* 价格 */
.price {
  font-size: 32rpx;
  color: #ff6b35;
}

.course-bottom{
  padding: 0 30rpx 30rpx 30rpx;
}
.check_in_status{
  font-size: 26rpx;
  color: #ff6b35;
  text-align: right;
}