/**
 * pages/addressList/addressList.wxss
 * 常用地址列表页面样式
 */

page {
  height: 100vh;
  background-color: #f5f5f5;
}

.container {
  padding: 20rpx;
  padding-bottom: 180rpx;
}

/* 地址列表 */
.address-list {
  margin-bottom: 20rpx;
}

.address-item {
  margin-bottom: 16rpx;
  overflow: hidden;
}

/* 左滑容器 */
.swipe-container {
  position: relative;
  border-radius: 16rpx;
  overflow: hidden;
}

.address-card {
  background-color: #fff;
  transition: transform 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
  position: relative;
  z-index: 2;
  width: 100%;
  height: 140rpx;
}

.address-card:active {
  box-shadow: 0 1rpx 12rpx rgba(0, 0, 0, 0.08);
}

.address-content {
  padding: 24rpx;
}

/* 左滑操作按钮 */
.swipe-actions {
  position: absolute;
  right: 0;
  top: 1rpx;
  width: 240rpx;
  height: 136rpx;
  display: flex;
  z-index: 1;
}

.swipe-btn {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  width: 120rpx;
  height: 100%;
  color: #fff;
  border: none;
  border-radius: 0;
  font-size: 24rpx;
  line-height: 1;
  padding: 0;
  position: relative;
  z-index: 3;
}

.swipe-btn::after {
  border: none;
}

.swipe-btn .iconfont {
  font-size: 28rpx;
  margin-bottom: 6rpx;
}

.btn-text {
  font-size: 20rpx;
  color: #fff;
}

.swipe-btn.set-default-btn {
  background-color: #007aff;
}

.swipe-btn.set-default-btn:active {
  background-color: #0056cc;
}

.swipe-btn.delete-btn {
  background-color: #ff3b30;
}

.swipe-btn.delete-btn:active {
  background-color: #d70015;
}

.address-header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  margin-bottom: 12rpx;
}

.address-name-row {
  display: flex;
  align-items: center;
  flex: 1;
  margin-right: 16rpx;
}

.address-name {
  font-size: 30rpx;
  color: #333;
  font-weight: 600;
  margin-right: 12rpx;
  max-width: 300rpx;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.default-tag {
  background-color: #007aff;
  color: #fff;
  padding: 2rpx 8rpx;
  border-radius: 8rpx;
  font-size: 18rpx;
  line-height: 1.2;
  flex-shrink: 0;
}

.default-text {
  font-size: 18rpx;
  color: #fff;
}

.address-actions {
  display: flex;
  gap: 8rpx;
  flex-shrink: 0;
}

.action-btn {
  padding: 6rpx 12rpx;
  border-radius: 12rpx;
  font-size: 22rpx;
  line-height: 1.2;
  border: none;
  min-width: 64rpx;
  text-align: center;
}

.action-btn::after {
  border: none;
}

.set-default-btn {
  background-color: rgba(0, 122, 255, 0.1);
  color: #007aff;
  border: 1rpx solid rgba(0, 122, 255, 0.3);
}

.set-default-btn:active {
  background-color: rgba(0, 122, 255, 0.15);
}

.delete-btn {
  background-color: rgba(255, 59, 48, 0.1);
  color: #ff3b30;
}

.delete-btn:active {
  background-color: rgba(255, 59, 48, 0.15);
}

/* 左滑删除按钮 */
.delete-action {
  position: absolute;
  right: 0;
  top: 0;
  bottom: 0;
  width: 120rpx;
  background-color: #ff3b30;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  color: #fff;
  z-index: 1;
}

.delete-action .iconfont {
  font-size: 32rpx;
  margin-bottom: 4rpx;
}

.delete-text {
  font-size: 20rpx;
  color: #fff;
}

.address-detail {
  margin-top: 8rpx;
}

.address-text {
  font-size: 24rpx;
  color: #666;
  line-height: 1.5;
  word-break: break-all;
}

/* 空状态 */
.empty-state {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 120rpx 40rpx;
  text-align: center;
}

.empty-icon {
  width: 120rpx;
  height: 120rpx;
  background-color: #f8f9fa;
  border-radius: 50%;
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 32rpx;
}

.empty-icon .iconfont {
  font-size: 60rpx;
  color: #ccc;
}

.empty-text {
  font-size: 32rpx;
  color: #333;
  font-weight: 600;
  margin-bottom: 12rpx;
}

.empty-desc {
  font-size: 26rpx;
  color: #999;
  line-height: 1.4;
}

/* 加载状态 */
.loading-state {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 80rpx 40rpx;
}

.loading-text {
  font-size: 28rpx;
  color: #999;
}

/* 新增地址按钮 */
.add-button-container {
  position: fixed;
  bottom: 60rpx;
  left: 50%;
  transform: translateX(-50%);
  width: calc(100% - 48rpx);
  max-width: 600rpx;
  z-index: 999;
}

.add-button {
  width: 100%;
  height: 96rpx;
  background: #333;
  color: #fff;
  font-size: 30rpx;
  font-weight: 600;
  border-radius: 48rpx;
  border: none;
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
  overflow: hidden;
}

.add-button:active {
  transform: translateY(1rpx);
  box-shadow: 0 4rpx 20rpx rgba(102, 126, 234, 0.2);
}

.add-button::after {
  border: none;
}

.add-button::before {
  content: '';
  position: absolute;
  top: 0;
  left: -100%;
  width: 100%;
  height: 100%;
  background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.1), transparent);
  transition: left 0.6s ease;
}

.add-button:active::before {
  left: 100%;
}

.add-button .iconfont {
  font-size: 28rpx;
}

.add-text {
  font-size: 30rpx;
  font-weight: 600;
} 